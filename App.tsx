import {
  StyleSheet,
  TouchableHighlight,
  TouchableOpacity,
  View,
} from 'react-native';
import React from 'react';
import SetUp from './src/routes/navigation/setup';

const App = () => {
  return <SetUp />;
};

export default App;

const styles = StyleSheet.create({});
